apiversion: extensions/v1beta1
kind: Deployment
metadata:
   name: deploy1
spec:
   replicas: 2
   template: 
      metadata:
        labels:
           app: app-v1
        spec:
          containers:
          - name: webcont
            image: gauravsun/customwebapp:v1
            ports:
            - containerPort: 80
